language: cpp
install: sudo add-apt-repository ppa:chris-lea/libsodium -y && sudo apt update && sudo apt install -y build-essential cmake pkg-config libboost-all-dev libssl-dev libunbound-dev libsodium-dev libminiupnpc-dev libunwind8-dev liblzma-dev libreadline6-dev libldns-dev libexpat1-dev doxygen graphviz libgtest-dev && cd /usr/src/gtest && sudo cmake . && sudo make . && sudo mv libg* /usr/lib